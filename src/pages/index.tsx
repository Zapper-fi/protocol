import React from "react";
import Layout from "@theme/Layout";
import { Intro } from "../modules/Intro/Intro";

const Home: React.FC = () => {
	return (
		<Layout>
			<Intro />
		</Layout>
	);
};

export default Home;
