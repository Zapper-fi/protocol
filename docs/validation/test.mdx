import { Tab, TabGroup, TabList, TabPanel, TabPanels } from '@headlessui/react'

# Under Construction 🚧

<TabGroup>
  <TabList className="text-center py-6">
    <Tab className="zapper-tab">Portfolio Tracking</Tab>
    <Tab className="zapper-tab">Human Readable-Transaction</Tab>
    <Tab className="zapper-tab">Onchain Identity</Tab>
    <Tab className="zapper-tab">Onchain Prices</Tab>
  </TabList>

  <TabPanels className="border-gray-100 border py-6 px-8 border-solid rounded-xl">
    <TabPanel>
      <TabGroup>  
        <TabList className="py-6">
          <Tab className="zapper-tab">Tokens</Tab>
          <Tab className="zapper-tab">Apps</Tab>
          <Tab className="zapper-tab">NFTs</Tab>
          <Tab className="zapper-tab">Totals</Tab>
          <Tab className="zapper-tab">Claimables</Tab>
        </TabList>

        <TabPanels>
          <TabPanel>
            ## Token Holdings

            Track token balances across all EVM chains with comprehensive coverage.

            Includes:
            - Real-time balance updates
            - Historical price data
            - Token metadata and images
            - Custom token lists support

            ```graphql
            query TokenHoldings($address: String!, $network: String!) {
              tokens(address: $address, network: $network) {
                symbol
                balance
                value
                price
                tokenInfo {
                  name
                  decimals
                  logo
                }
              }
            }
            ```
          </TabPanel>
          <TabPanel>
            ## Access DeFi Holdings

            Leverage Zapper's 2,000+ bespoke DeFi integrations for any EVM address.

            Includes:
            - 99% DeFi Balance TVL Coverage
            - 90,000+ DeFi positions
            - Any user-contributed DeFi integrations appear seamlessly in the API response once approved

            ```graphql
            query DeFiPositions($address: String!) {
              defiPositions(address: $address) {
                protocol
                positionType
                balance
                value
                rewards {
                  symbol
                  amount
                }
              }
            }
            ```
          </TabPanel>
          <TabPanel>
            ## NFT Portfolio

            Complete NFT portfolio tracking across major marketplaces.

            Features:
            - Floor price tracking
            - Rarity scores
            - Historical trading data
            - Metadata enrichment

            ```graphql
            query NFTHoldings($address: String!) {
              nfts(address: $address) {
                collection
                tokenId
                floorPrice
                rarity
                lastSalePrice
                metadata {
                  name
                  image
                  attributes
                }
              }
            }
            ```
          </TabPanel>
          <TabPanel>
            ## Portfolio Analytics

            Comprehensive portfolio analytics and performance metrics.

            Includes:
            - Total portfolio value
            - Historical performance
            - Risk metrics
            - Asset allocation

            ```graphql
            query PortfolioTotals($address: String!) {
              portfolio(address: $address) {
                totalValue
                performance {
                  daily
                  weekly
                  monthly
                }
                allocation {
                  category
                  percentage
                }
              }
            }
            ```
          </TabPanel>
          <TabPanel>
            ## Claimable Rewards

            Track and manage claimable rewards across protocols.

            Features:
            - Airdrops
            - Governance tokens
            - Staking rewards
            - Yield farming returns

            ```graphql
            query ClaimableRewards($address: String!) {
              claimables(address: $address) {
                type
                protocol
                token
                amount
                value
                claimDeadline
              }
            }
            ```
          </TabPanel>
        </TabPanels>
      </TabGroup>
    </TabPanel>

    <TabPanel>
      <TabGroup>
        <TabList className="py-6">
          <Tab className="zapper-tab">Single Transaction</Tab>
          <Tab className="zapper-tab">Account Timeline</Tab>
          <Tab className="zapper-tab">App Timeline</Tab>
        </TabList>

        <TabPanels>
          <TabPanel>
            ## Transaction Details

            Human-readable transaction details for any on-chain activity.

            Features:
            - Plain English descriptions
            - Token movements
            - Gas costs
            - Contract interactions

            ```graphql
            query TransactionDetails($txHash: String!) {
              transaction(hash: $txHash) {
                description
                value
                gasUsed
                tokens {
                  token
                  amount
                  direction
                }
                interactions {
                  protocol
                  action
                }
              }
            }
            ```
          </TabPanel>
          <TabPanel>
            ## Account History

            Comprehensive timeline of account activity.

            Includes:
            - Chronological transaction history
            - Interaction patterns
            - Value flows
            - Protocol usage

            ```graphql
            query AccountTimeline($address: String!, $startTime: Int!, $endTime: Int!) {
              accountActivity(
                address: $address
                startTime: $startTime
                endTime: $endTime
              ) {
                transactions {
                  timestamp
                  type
                  value
                  description
                }
              }
            }
            ```
          </TabPanel>
          <TabPanel>
            ## Protocol Activity

            Detailed timeline of interactions with specific protocols.

            Features:
            - Protocol-specific analytics
            - Usage patterns
            - Historical interactions
            - Performance metrics

            ```graphql
            query ProtocolTimeline($protocol: String!, $address: String!) {
              protocolActivity(protocol: $protocol, address: $address) {
                interactions {
                  timestamp
                  action
                  value
                  success
                }
                metrics {
                  totalValue
                  frequency
                }
              }
            }
            ```
          </TabPanel>
        </TabPanels>
      </TabGroup>
    </TabPanel>

    <TabPanel>
      ## Onchain Identity Analysis

      Comprehensive identity resolution and analytics.

      Features:
      - ENS integration
      - Cross-chain identity linking
      - Activity clustering
      - Behavioral analytics

      ```graphql
      query OnchainIdentity($address: String!) {
        identity(address: $address) {
          ens
          linkedAddresses
          activityClusters {
            type
            frequency
            value
          }
          behaviorMetrics {
            tradingPattern
            protocolPreferences
            riskProfile
          }
        }
      }
      ```
    </TabPanel>

    <TabPanel>
      ## Market Data & Pricing

      Real-time and historical price data across assets.

      Includes:
      - Token prices
      - NFT floor prices
      - LP token pricing
      - Historical price feeds

      ```graphql
      query AssetPrices($tokens: [String!]!, $timeRange: String!) {
        prices(tokens: $tokens, timeRange: $timeRange) {
          token
          current
          historical {
            timestamp
            price
            volume
          }
          marketCap
          priceChange24h
        }
      }
      ```
    </TabPanel>
  </TabPanels>
</TabGroup>