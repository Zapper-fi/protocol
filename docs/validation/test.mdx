import { Tab, TabGroup, TabList, TabPanel, TabPanels } from '@headlessui/react'

# Under Construction 🚧

<TabGroup>
  <TabList className="text-center py-6">
    <Tab className="zapper-tab">Portfolio Tracking</Tab>
    <Tab className="zapper-tab">Human Readable-Transaction</Tab>
    <Tab className="zapper-tab">Onchain Identity</Tab>
    <Tab className="zapper-tab">Onchain Prices</Tab>
  </TabList>

  <TabPanels className="border-gray-100 border py-6 px-8 border-solid rounded-xl">
    <TabPanel>
      <TabGroup>  
        <TabList className="py-6">
          <Tab className="zapper-tab">Tokens</Tab>
          <Tab className="zapper-tab">Apps</Tab>
          <Tab className="zapper-tab">NFTs</Tab>
          <Tab className="zapper-tab">Totals</Tab>
          <Tab className="zapper-tab">Claimables</Tab>
        </TabList>

        <TabPanels>
          <TabPanel>
            ## Access Token Balances

            Surfaces any onchain token balances held by an address. [Learn more](/docs/tokens)

            - Real-time balance updates
            - Token metadata and prices
            - Bullet 3

            ```graphql
            query TokenBalances($address: String!) {
              tokens(address: $address) {
                balance
                token {
                  name
                  symbol
                  metadata
                }
              }
            }
            ```
          </TabPanel>

          <TabPanel>
            ## Access DeFi holdings

            Leverage Zapper's 2,000+ bespoke DeFi integrations for any EVM address. [Learn more](/docs/apps)

            - 99%+ DeFi Balance TVL Coverage
            - 90,000+ DeFi positions
            - Any user-contributed DeFi integrations appear seamlessly in the API response once approved

            ```graphql
            query DefiHoldings($address: String!) {
              apps(address: $address) {
                protocol
                balance
                positions
              }
            }
            ```
          </TabPanel>

          <TabPanel>
            ## Estimated value & metadata for every NFT Collection

            Detailed data with for NFT collections you care about across 20+ chains. [Learn more](/docs/nfts)

            - Full Marketplace data
            - Estimated Values based on historical sales and traits
            - Complete metadata with high-quality media files, thumbnail, and blurhash

            ```graphql
            query NFTCollections($address: String!) {
              nfts(address: $address) {
                collections
                value
                metadata
              }
            }
            ```
          </TabPanel>

          <TabPanel>
            ## Access Total Onchain Net Worth

            Surfaces various aggregations of onchain portfolio data. [Learn more](/docs/totals)

            - Across 35+ chains
            - Bullet Point 2
            - Bullet Point 3

            ```graphql
            query TotalNetWorth($address: String!) {
              totals(address: $address) {
                netWorth
                chains
                breakdown
              }
            }
            ```
          </TabPanel>

          <TabPanel>
            ## Delight users with Claimables

            Surfaces all available claimable tokens for a given address across all indexed onchain apps. [Learn more](/docs/claimables)

            - Across 90,000+ DeFi positions
            - Bullet Point 2
            - Bullet Point 3

            ```graphql
            query Claimables($address: String!) {
              claimables(address: $address) {
                tokens
                amounts
                protocols
              }
            }
            ```
          </TabPanel>
        </TabPanels>
      </TabGroup>
    </TabPanel>

    <TabPanel>
      <TabGroup>
        <TabList className="py-6">
          <Tab className="zapper-tab">Single Transaction</Tab>
          <Tab className="zapper-tab">Account Timeline</Tab>
          <Tab className="zapper-tab">App Timeline</Tab>
        </TabList>

        <TabPanels>
          <TabPanel>
            ## Onchain transactions made simple

            Summarizes onchain transactions with optional references to apps, tokens, NFTs, and accounts. Ideal for activity feeds or detailed transaction views. [Learn more](/docs/transactions)

            - Bullet 1
            - Bullet 2
            - Bullet 3

            ```graphql
            query Transaction($hash: String!) {
              transaction(hash: $hash) {
                summary
                references
                details
              }
            }
            ```
          </TabPanel>

          <TabPanel>
            ## Explore Onchain Activity at a Glance

            Presents the onchain activity of a specific address(s) in a simple descriptive summary with references to dynamic elements of the transaction such as tokens, NFTs, and accounts. [Learn more](/docs/timeline)

            - Bullet 1
            - Bullet 2

            ```graphql
            query AccountActivity($address: String!) {
              timeline(address: $address) {
                activities
                references
                elements
              }
            }
            ```
          </TabPanel>

          <TabPanel>
            ## App Activity Feeds

            Displays a timeline of transactions that happened in a particular onchain app via their smart contracts. [Learn more](/docs/app-activity)

            - Bullet 1
            - Bullet 2

            ```graphql
            query AppActivity($appId: String!) {
              appTimeline(appId: $appId) {
                transactions
                contracts
                activity
              }
            }
            ```
          </TabPanel>
        </TabPanels>
      </TabGroup>
    </TabPanel>

    <TabPanel>
      ## Build with Onchain Identity

      Enrich your app by surfacing onchain social identity such as avatars, ENS, Farcaster, and more. [Learn more](/docs/identity)

      - Bullet 1
      - Bullet 2

      ```graphql
      query OnchainIdentity($address: String!) {
        identity(address: $address) {
          avatars
          ens
          social
        }
      }
      ```
    </TabPanel>

    <TabPanel>
      ## A price for every token, from memecoins to blue chips

      Onchain token prices, current and historical, for any token with an onchain market. [Learn more](/docs/prices)

      - Price average across multiple price sources instead of by largest price source
      - 2.5M+ Token Prices and balances with hundreds more added each day
      - Instantly and automatically detects newly deployed price sources

      ```graphql
      query TokenPrices($tokens: [String!]!) {
        prices(tokens: $tokens) {
          current
          historical
          sources
        }
      }
      ```
    </TabPanel>
  </TabPanels>
</TabGroup>